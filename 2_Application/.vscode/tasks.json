{
    "tasks": [
        {
            "label": "make clean",
            "type": "shell",
            "command": "make", 
            "args": ["clean"],           //执行make命令（注，文件夹下需要Makefile文件）     
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "make build",
            "type": "shell",
            "command": "make",            //执行make命令（注，文件夹下需要Makefile文件）     
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "dependsOn":["make clean"]
        }
    ],
    "version": "2.0.0"
}